cd $(dirname $0)

curl -sL https://github.com/btwiuse/vscodium/releases/download/1.96.4.25029/vscodium-reh-linux-x64-1.96.4.25029.tar.gz | tar xz

[[ -n $RAILWAY_ENVIRONMENT ]] && {
  ufo term &
  ./bin/codium-server --host 0.0.0.0 --port $PORT --without-connection-token
} || {
  true
}
